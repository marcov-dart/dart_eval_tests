name: test_eval
description: A sample command-line application.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.11.5

# Add regular dependencies here.
dependencies:
  dart_eval:
    path: ../dart_eval/
  path: ^1.9.0
  eval_annotation: ^0.8.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
